services:
  postgres:
    image: postgres
    environment:
      POSTGRES_PASSWORD: "rock4me!!"
      POSTGRES_DB: "louvorappdb"
    ports:
      - "5436:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build:
      context: .
      dockerfile: Dockerfile
    # Build the schema from migrations on boot (replaces the old db.sql init),
    # then start the dev server with hot reload.
    command: sh -c "npm run migration:up && npm run start:dev:docker"
    environment:
      DATABASE_NAME: "louvorappdb"
      DATABASE_USER: "postgres"
      DATABASE_PASSWORD: "rock4me!!"
      DATABASE_HOST: "postgres"
      DATABASE_PORT: "5432"
      JWT_SECRET: "SECRET"
    ports:
      - "3000:80"
    volumes:
      # Bind-mount only the source for hot reload. node_modules (with its
      # linux-native bcrypt/swc) and dist (built fresh by nest --watch) stay
      # inside the container from the image build — never overlaid by the host.
      - ./src:/app/src
    depends_on:
      postgres:
        condition: service_healthy